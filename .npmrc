# next-auth@5 beta declares an optional peer on nodemailer ^7 while the site
# uses nodemailer 8; npm refuses to install without this (ERESOLVE). Vercel
# reads this file too, so the production install matches local.
legacy-peer-deps=true
